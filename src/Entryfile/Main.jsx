import { BrowserRouter } from "react-router-dom";
import App from "../initialpage/App";

const Main = () => {
  return (
    <BrowserRouter> 
      <App />
    </BrowserRouter>
  );
};

export default Main;
