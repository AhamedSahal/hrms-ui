import { postWithAuth } from "../../../HttpRequest";

const servicePath = '/my-pay';

export function getEmployeemonthlySalary(salaryMonth) {
    // let path = `${servicePath}?monthlySalary=${salaryMonth}`;
    // return postWithAuth(path).then(res => {
    //     return Promise.resolve(res.data);
    // }).catch(err => {
    //     return Promise.reject(err);
    // });
}

export function getMyPayslips(salaryMonth) {
    // let path = `${servicePath}?monthlySalary=${salaryMonth}`;
    // return postWithAuth(path).then(res => {
    //     return Promise.resolve(res.data);
    // }).catch(err => {
    //     return Promise.reject(err);
    // });
}